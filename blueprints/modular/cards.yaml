title: Cards
'@extends':
    type: default
    context: blueprints://pages

form:
  fields:
    tabs:
      fields:
        options:
          fields:
            section:
              type: section
              title: Styles
              underline: true
              import@:
                type: partials/styles
                context: blueprints://pages
