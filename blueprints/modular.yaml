title: Metadata
'@extends':
    type: modular
    context: blueprints://pages

form:
  fields:
    tabs:
      fields:

        options:
          fields:
            section:
              type: spacer
              title: Advanced
            header.onpage_menu:
              label: On Page Menu
              type: toggle
              highlight: 1
              default: 1
              options:
                1: Enabled
                0: Disabled
              validate:
                type: bool
