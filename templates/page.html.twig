{% extends 'partials/base.html.twig' %}

{% set image = page.media.images|first %}

{% block content %}

	<div class="row">

		<div class="col-3-4">
			{{ image.cropZoom(800,280).html }}
			<h1>{{ page.title }}</h1>
			{{ page.content }}
		</div>

		<div class="col-1-4">
			<img src="https://placeholdit.imgix.net/~text?txtsize=33&txt=sidebar&w=350&h=150&bg=0065ff&txtcolor=fff" alt="sidebar" />

			<h3>Related</h3>
		</div>

	</div>

{% endblock %}
